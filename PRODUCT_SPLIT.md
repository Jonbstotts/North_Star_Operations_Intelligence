# Historical Product Split

North Star Operations Intelligence is now fully standalone. The runtime no longer contains the former shared weather/traffic application branch, alternate dashboard launcher, shared data-directory migration, or legacy administrator-authentication migration.

North Star reads and writes only its own application-data directory: `~/.northstar-operations-intelligence`.
